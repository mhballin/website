---
import BaseLayout from "../layouts/BaseLayout.astro";
import { projects } from "../data/projects";

const categories = ["blueprint", "making", "creative"];
const categoryNames = {
  blueprint: "Blueprint",
  making: "Making",
  creative: "Creative"
};
---

<BaseLayout title="Projects & Work" sideBarActiveItemID="work">
  <div class="pb-12">
    <div class="text-3xl w-full font-bold">Projects & Work</div>
    <p class="text-lg text-gray-600 mt-4">
      I have always been someone who loves to create things. As a child, I was constantly building,
      tinkering, and experimenting. This passion for making has only grown stronger over the years,
      leading me to explore a variety of creative outlets. Below are some of my favorite projects 
      that I've made.
  </div>

  <!-- Category Tiles with Hero Images -->
  <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-16">
    {categories.map((cat) => {
      const project = projects.find(p => p.category === cat);
      return (
        <a href={`/work/${cat}`} class="group" aria-label={`View ${categoryNames[cat]} projects`}>
          <div class="relative h-64 bg-base-200 rounded-lg overflow-hidden hover:shadow-lg transition-all duration-300">
            {project ? (
              <>
                <img 
                  src={project.heroImage} 
                  alt={project.title ?? categoryNames[cat]}
                  class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
                />
                <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent"></div>
              </>
            ) : (
              <div class="w-full h-full bg-gradient-to-tr from-gray-700 to-gray-900 flex items-center justify-center">
                <span class="text-white text-3xl font-bold">{categoryNames[cat]}</span>
              </div>
            )}
            <div class="absolute inset-0 flex items-end p-6">
              <h3 class="text-3xl font-bold text-white">{categoryNames[cat]}</h3>
            </div>
          </div>
        </a>
      );
    })}
  </div>
</BaseLayout>
